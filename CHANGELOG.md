# 更新日志

本文件记录每个版本的变更，以及**兼容性承诺的边界**。
格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)。

---

## ⚠️ 关于兼容性

**当前为测试版（alpha），不承诺向后兼容。** 后续版本**可能包含破坏兼容性的改动**，
且这类改动在 alpha 阶段**不会**做成渐进弃用，可能直接变更。

具体可能变的东西（**升级前请固定 commit 或 tag**）：

| 类别 | 举例 |
|---|---|
| **配置格式** | `00_配置/engines.json`、`courses.json`、`references.json`、`page-offsets.json`、`experimental-batches.json` 的键名与结构 |
| **产物契约** | `20_文本库/pages.jsonl`、`30_知识点/kp.jsonl` / `evidence.jsonl` / `audit.jsonl` / `skeleton.json` 的字段（增删改名都可能） |
| **CLI 参数** | `python -m firekb <环节>` 的环节名、各脚本的参数与退出码 |
| **目录结构** | 数据目录名与分层（当前是 `10_原始归档` / `20_文本库` / `30_知识点` / `40_复习产物` / `90_日志`） |
| **状态文件** | `state_*.json` 的键格式（**升级后可能需要迁移**，见 `python -m firekb migrate`） |

**对使用者的建议**

1. **要稳定就用 tag**：`git checkout v0.1.0-alpha`，别直接跟 `main`。
2. **升级后先跑自检**：`python -m firekb doctor` —— 它会把契约不符、口径裂缝报出来。
3. **升级前备份数据**：`20_文本库/` 与 `30_知识点/` 是你的成果，不是代码的一部分。
4. **语料可重跑**：产物都能由素材重建（这正是本项目"任何结论落到文件+页码"的设计前提），
   但 LLM 抽取**要花钱**，所以升级前先想清楚是否值得重跑。
5. 版本号用 semver 语义：`0.x` 阶段的 minor 版本**允许**破坏性改动。

---

## [Unreleased]

### 修复

- **统一入口不再吞掉错误消息**：引擎与各守卫用 `SystemExit("原因")` 拒绝开工时，
  消息此前会被运行记录层拦下而**无处输出** —— 于是 `python -m firekb parse` 表现为
  「零输出 + 退出码 1」，用户拿不到任何可排查的线索（直接运行 `tools\` 下的脚本却能看到）。
  现在消息会原样转发到 `stderr`，控制流不变。
- **依赖体检跟随配置**：`check_env` 不再把 `pymupdf` 列为必需 —— 默认解析引擎用的是
  `pypdf` + `pypdfium2`。此前照 README 装完依赖的人永远拿不到退出码 `0`，
  还会被推着引入 AGPL-3.0 组件（与本项目的分发承诺冲突）。
- **各环节只校验自己用到的引擎**：M1 解析不再因为缺 OCR / 转写依赖而拒绝开工
  （文档里那两者标的本就是「可选」）。
- **M2 OCR 的句柄关闭**改为走引擎接口：此前它在处理完一个文件后会抛 `NameError`。
- **`status` 不再把「校验全部通过」显示成「尚未校验」**：判定改为看知识点的核验状态；
  复核队列为空时明确显示「0 条待人工复核」（那是最理想的状态，不是没做）。
- **有失败项时退出码为 `4`**：M1 解析、M4b 抽取、`testdata` 此前是"打印失败清单但返回 0"。
- **自测中断时也给出汇总**（已通过多少项），不再只留一段回溯让人误判成"失败了很多项"；
  同时修掉自测自己留下的临时目录（此前每跑一次就在系统临时目录里多一个目录，不会回收）。
- **公开 README 补齐上手路径**：虚拟环境的用法、先用合成样例（`firekb testdata`）跑通全链路、
  `ffmpeg` 必须命令行可用、各可选依赖分别服务哪个环节、以及环节脚本的退出码含义。

---

## [0.1α] - 2026-09-26

首个公开快照。**测试版**，功能可用但接口未冻结。

### 核心能力

- **可溯源流水线**：素材（PDF / PPTX / EPUB / 课堂录音）→ 页级文本 → 知识骨架 →
  知识点与证据 → 核验与人工复核队列 → 复习产物。
- **引用四档核验**：`exact` / `partial` / `fabricated` / `not_found`，
  **只有 `exact` 算通过**；数字走严格通道（`1.5` 与 `15` 必须区分）。
- **页码双轨**：印刷页码 ↔ PDF 物理页 / EPUB 节序号，支持偏移量与锚点插值两种模式；
  没有映射规则时**显式退化为 `PDF pN`**，绝不冒充印刷页。
- **多口径一致性台账**：把课表 / 素材 / 文本 / 骨架 / 知识点 / 证据 / 规范条文库
  摊成一张表，口径裂缝当场报出。
- **运行可追溯**：每条产物行带 `run_id`，每个环节的起止、耗时、成本、模型与引擎指纹
  记入 `90_日志/runs.jsonl`。

### 工程设施

- **统一入口**：`python -m firekb <环节>`（27 个环节），也可直接调用 `tools/` 下的脚本。
- **引擎层可替换**：解析 / OCR / 转写三类能力接口化，实现由 `00_配置/engines.json` 选择。
- **首次初始化**：`python -m firekb init` —— 建目录、从模板生成配置（幂等，不覆盖）。
- **退出码分档**：`0` 全过 / `7` 尚未初始化 / `6` 仅可选引擎未就绪 / `5` 硬门槛失败。
- **单元自测**：`python -m firekb selftest`（共享原语 + 引擎层 + 出境纪律；项数随开发增长，
  跑一下就能看到当前数量）。

### 许可与合规

- 代码 **MIT**。
- **默认依赖栈全部为宽松许可**（`pypdf` BSD-3、`pypdfium2` Apache-2.0/BSD-3、
  `rapidocr-onnxruntime` Apache-2.0、`python-pptx` MIT）
  ⇒ **打包分发应用不受 copyleft 约束**。
- 唯一 copyleft 组件 `PyMuPDF`（AGPL-3.0）降级为**需显式启用**的可选引擎。
  边界见 `THIRD-PARTY-NOTICES.md`。
- **数据出境纪律**：个性化数据（录音 / 转写 / 复习产物 / 笔记 / 作答记录）
  **绝不出境**（硬规则，同意也不放行）；第三方版权书目为**书面禁令 + 不做技术限制**。

### 已知限制

- 判断类任务（"这条引用是不是编的"）**必须由人复核** ——
  实测本地 4B 规模模型与人工标注一致率仅 35%、幻觉标记精度 40%。
  项目提供的是**让人更快复核**的工具（引用与原文同屏对照），不是替代人。
- 输出质量受素材文本层制约：影印版需先 OCR；公式与表格的抽取质量明显低于正文。
- `pypdf` 取文字为纯 Python 实现，比 PyMuPDF 慢约 10 倍（385 页约 30s）。
- 面向中文教材调校；英文教材可用但需自行验证。

