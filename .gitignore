# ============================================================
# FireKB .gitignore
# 原则：**代码、配置模板、文档入库；素材、派生数据、日志、密钥一律不入库**
#   · 入库：tools/*.py、tools/主力机配置工具/*、README.md、00_配置/*.md、
#           00_配置/courses.json、00_配置/.env.example、本文件
#   · 不入库的东西**全部可由工具重建**（素材→解析→骨架→抽取→校验→产物）
# ============================================================

# ---------- 密钥与凭据（永不入库）----------
00_配置/.env
00_配置/.egress-consent.json
*.credentials.yaml
*.key
*.pem

# ---------- 版权素材（教材/规范/课件/录音，约 197 MB，永不入库）----------
# 含第三方出版物与课堂录音：涉及著作权与讲课人权益，禁止进版本库、禁止随包分发
10_原始归档/

# ---------- 派生数据（可由工具重建，永不入库）----------
# pages.jsonl / kp.jsonl / evidence.jsonl / audit.jsonl / skeleton.json /
# transcript.jsonl / 40_复习产物 全部属此类
20_文本库/
30_知识点/
40_复习产物/

# ---------- 日志 / 版本快照 / 口径归档（永不入库）----------
90_日志/

# ---------- 第三方二进制（体积 295 MB + 许可证义务，永不入库）----------
# ffmpeg 由主力机单独安装；分发它会触发其构建对应的 GPL/LGPL 义务
tools/ffmpeg/

# ---------- 共享裸库自身（origin 就放在项目根下的 _git/，不能被递归跟踪）----------
_git/

# ---------- 运行环境与缓存 ----------
.venv/
__pycache__/
*.pyc
*.pyo

# ---------- 临时产物 / 备份 ----------
*.bak
*.pre-*.bak
*.restored.py
s3_*.out
s3_*.err
*~
*.tmp
*.log

# ---------- 编辑器 / 系统 ----------
.vscode/
.idea/
Thumbs.db
desktop.ini
