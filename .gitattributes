# 本项目两台机器以「逐字节同源」为硬约束：
#   · tools\sync.py 用 MD5 逐文件比对，要求两侧字节一致
#   · 版本快照 / 归档 / 冻结三件套都按 SHA256 审计
# 因此**禁止 git 做任何换行或编码转换**（* -text = 一律按原始字节存取）。
# 如果哪天要做跨平台协作，先改这条并同步重算所有哈希，不要临时放开。
* -text

# diff 时对文本文件仍按文本展示（只影响显示，不影响存取）
*.py diff
*.md diff
*.json diff
*.yml diff
*.ps1 diff
