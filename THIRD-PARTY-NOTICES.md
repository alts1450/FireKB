# 第三方依赖与许可清单（THIRD-PARTY NOTICES）

> **本项目的代码许可**：见 `LICENSE`（MIT）。
> **本文件解决的是另一件事**：FireKB **依赖或调用了哪些第三方组件、它们各自的许可是什么、
> 以及"什么情况下会触发义务"**。
>
> 为什么要单独写一份：MIT 只覆盖**本仓库自己的代码**。第三方组件的义务由它们各自的许可决定，
> **不因本项目采用 MIT 而消失**。尤其是 PyMuPDF 的 AGPL-3.0 —— 它在"对外分发"或"提供网络服务"
> 这两种场景下会传导到整个服务端，而自用不触发。把这条写下来，是为了避免将来"以为 MIT 就万事大吉"。

**结论先行**

- 本项目**代码**为 MIT（见 `LICENSE`）。
- **默认依赖栈全部是宽松许可** —— PDF 解析用 `pypdf`(BSD-3) + `pypdfium2`(Apache-2.0/BSD-3)，
  OCR 用 `rapidocr-onnxruntime`(Apache-2.0)，PPTX 用 `python-pptx`(MIT)。
  ⇒ **打包成应用分发是自由的**，不触发任何 copyleft 义务。
- 依赖**不随本仓库分发**（由使用者自行安装），因此它们的许可义务也不传导到本仓库代码。
- ⚠️ 仍然有**一种情况会改变结论** —— 如果你**主动**启用了 AGPL 组件（下表标 ⚠️ 的那一行）：

  > ### ⚠️ 使用 PyMuPDF 引擎 + 打包分发 = 你在分发 AGPL 组件
  >
  > 本项目默认**不使用** PyMuPDF。但如果你在 `00_配置/engines.json` 里把 `parser`
  > 改成 `local_pymupdf`，然后**把内含它的程序打包分发出去**（PyInstaller 之类），
  > 你就在**分发 AGPL 组件** —— 整个组合须按 AGPL 履行义务（提供对应源码等）。
  >
  > 那时只有两条路：**换回默认的 `local_permissive`**，或**把本项目整体改为 AGPL-3.0**。
  >
  > 只发源码、让使用者自行 `pip install` 依赖 —— **不触发**这一条。

---

## 一、Python 依赖

| 组件 | 许可 | 用法 | 是否随本项目分发 | 义务状态 |
|---|---|---|---|---|
| **pypdf** | **BSD-3-Clause** | PDF 取文字（默认 `parser` 引擎 `local_permissive`） | 否 | 无 |
| **pypdfium2**（含 PDFium 二进制） | **Apache-2.0 / BSD-3** | PDF 渲染成图（给扫描页 OCR 用），默认 `parser` 引擎 | 否 | 无 |
| rapidocr-onnxruntime | Apache-2.0 | 扫描页 OCR（`ocr` 引擎） | 否 | 无（保留声明即可） |
| PP-OCR 内置模型（随 rapidocr 分发） | Apache-2.0 | OCR 识别模型 | 随 rapidocr 包 | 无 |
| onnxruntime | MIT | OCR 推理运行时 | 否 | 无 |
| numpy | BSD-3-Clause | OCR 图像处理 | 否 | 无 |
| Pillow | MIT-CMU | 图像互转（渲染 → OCR） | 否 | 无 |
| python-pptx | MIT | PPTX 解析 | 否 | 无 |
| reportlab | BSD-3-Clause | 生成样例 PDF（`tools/make_testdata.py`） | 否 | 无 |
| **faster-whisper** | MIT | 语音转写（`asr` 引擎） | 否 | 无 |
| **CTranslate2** | MIT | faster-whisper 推理后端 | 否 | 无 |
| **Whisper 模型权重** | **须到来源页核实** | 转写模型 | 否 | ⚠️ **在核实之前，不得在对外材料中对其许可做任何表述** |
| **PyMuPDF** (`pymupdf`) | **AGPL-3.0** 或 Artifex 商业双许可 | PDF 解析（**可选的** `parser` 引擎 `local_pymupdf`，默认**不使用**） | **否**（由使用者自行 `pip install`） | ⚠️ **自用无义务**；一旦启用它并对外分发程序，整个组合须按 AGPL 履行义务，或购买 Artifex 商业许可 |

> 说明：本项目**不 vendor（随包分发）任何第三方 Python 包**。依赖由使用者在自己的环境安装，
> 因此"分发本项目代码"≠"分发这些组件"。这一条是引擎层接口化（`tools/engines/`）的直接收益：
> 换实现不改业务脚本，合规义务也随之留在使用者一侧。
>
> **默认引擎刻意选了宽松许可的组合**，所以"打包分发应用"这条最常走的路默认是通的；
> 唯一带 copyleft 的组件（PyMuPDF）降级为**需要显式启用**才生效的可选项。

## 二、外部可执行文件

| 组件 | 许可 | 用法 | 是否随本项目分发 | 义务状态 |
|---|---|---|---|---|
| **ffmpeg**（GPLv3 构建） | **GPLv3**（构建相关） | 音频转码（转写前置步骤），**只作运行时依赖** | **否**（由使用者自行安装；`tools/ffmpeg/` 已在 `.gitignore` 中） | ⚠️ **分发它会触发其构建对应的 GPL/LGPL 义务**；仅调用、不分发则不触发 |

## 三、外部服务（数据出境）

| 服务 | 用法 | 数据出境 |
|---|---|---|
| DeepSeek API（`https://api.deepseek.com`） | 知识点抽取 / 引用核验（`extract` / `verify` 环节） | **会**：把教材分块正文发送到该服务 |

出境把关机制：`tools/engines/egress.py` + `00_配置/.egress-consent.json`（**本机私有，已 gitignore**）。
`00_配置/engines.json` 当前配置的 **parser / ocr / asr 三类引擎全部在本地执行**（`数据出境：none`）；
只有 LLM 环节会出境，且这部分走 `.env` 里的自备密钥（BYOK）。

## 四、语料（不是软件，但必须写明）

`10_原始归档/`（教材、规范、课件、课堂录音，约 197 MB）是**第三方版权材料与讲课人录音**，
`.gitignore` 中整目录排除：**不入版本库、不随包分发**。
本项目的产物（`pages.jsonl` / `kp.jsonl` 等）**包含教材原文片段**（引用核验需要逐字比对），
因此**派生数据同样不对外分发**（`20_文本库/`、`30_知识点/`、`40_复习产物/` 均已 gitignore）。

出境纪律见下一节 —— **版权书目的书面禁令在那里**（个性化数据的硬规则也在那里）。

---

## 五、出境纪律（两类数据，两种处理）

本项目对"数据出境"分两类，**处理方式完全不同** —— 混淆两者是本项目最容易犯的错：

| 类别 | 范围 | 处理 |
|---|---|---|
| **个性化数据** | 课堂录音、转写文本、复习产物、笔记、错题、作答记录 | **绝不出境（硬规则）**：同意与 `--allow-egress` 都**不放行**，由代码拦死 |
| **第三方版权书目** | 教材 / 规范 / 课件的原始文件、页级文本、页面图像 | **书面禁令 + 不做技术限制**（见下） |

### 5.1 个性化数据：绝不出境（硬规则）

- **代码位置**：`tools/engines/egress.py` 的 `NEVER_EGRESS = {audio, transcript, personal}`；
  `require_consent()` **先查这一层**，命中即无条件拒跑；`grant()` 也**拒绝**为这类引擎记录同意。
- **理由**：同意机制回答的是"你有没有**权利**上传"（版权）——那可以由人授权；
  而个性化数据记录的是**这个人自己的学习痕迹**，一旦出境无法收回，不该由一次按钮决定。

### 5.2 第三方版权书目：书面禁令，不做技术限制

> ## ⚠️ 书面禁令
>
> **禁止直接将未获得版权的书目发往云端。违反者后果自负。**
>
> 本工具**不做技术限制**：同意流程与 `--allow-egress` 照常有效。

- **为什么不技术限制**：判断"这本书目有没有获得版权"需要人掌握的现实信息（来源、授权状态），
  代码判不了。做一个判错的硬拦，比诚实写一条禁令更糟 —— 它会给人"已经合规了"的错觉。
- **项目负责什么**：把话说清楚并留痕。每次有出境引擎要放行时，`require_consent()` 都会打印上面这条禁令。
- **责任人**：上传与否、后果如何，由使用人承担。

---

## 六、对外发布 / 打包分发前的处理清单

按默认配置（`local_permissive` + `local_rapidocr`）打包分发**不需要**先做任何许可处理 ——
默认栈全是宽松许可。下面这几条只在**你改动了配置**或**要对外提供服务**时才需要处理：

1. **PyMuPDF**：只在 `engines.json` 里显式启用它时才有这个问题。届时要么换回默认实现，
   要么购买 Artifex 商业许可，要么明确以 AGPL 发布整个组合。
2. **ffmpeg**：仍作运行时依赖（不分发），并在文档中写明使用者需自行安装。
3. **Whisper 模型权重**：到来源页核实许可后再做任何对外表述。
4. **语料与其派生数据**：确认不含可识别的第三方版权内容，或干脆不分发数据。
5. **出境同意**：若对外提供服务，`.egress-consent.json` 属于**使用者本机状态**，不得随包分发。

---

*本清单随依赖变化维护。新增依赖时，请同步更新本文件 —— 尤其注意别把 copyleft 组件
放进**默认**路径：默认路径决定了使用者能否自由分发打包好的应用。*
